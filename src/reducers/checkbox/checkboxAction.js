import { createAction } from "@reduxjs/toolkit";

export const allTransfers = createAction('ALL_TRANSFERS');

export const directTransfer = createAction('DIRECT_TRANSFERS');

export const oneTransfer = createAction('ONE_TRANSFER');

export const twoTransfer = createAction('TWO_TRANSFER');

export const threeTransfer = createAction('THREE_TRANSFER');
